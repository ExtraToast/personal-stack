# auth-api

## OpenAPI contract

`services/auth-api/openapi.json` is generated by running `./gradlew :services:auth-api:exportOpenApiSpec` or `services/auth-api/scripts/generate-openapi.sh`. The committed contract is verified in CI by the `contract-validate` workflow and must be regenerated whenever controllers change.
