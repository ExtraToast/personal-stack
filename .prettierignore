node_modules
dist
build
*.min.*
pnpm-lock.yaml
coverage
stryker-tmp
playwright-report
**/generated/
**/generated-jooq/
# OpenAPI contract artefacts (pinned spec + openapi-typescript output).
# Regenerated by `./gradlew :services:assistant-api:exportOpenApiSpec`
# and `pnpm --filter @personal-stack/assistant-ui contract:generate`.
services/assistant-api/openapi.json
**/src/api/generated.ts
.gradle

# Agent-kit content: prompts, templates, rendered skills, the manifest, and
# design notes are authored/generated text (and prompt wording), not JS sources.
# Keep prettier off them so it neither churns prompt text nor fights the
# agent-kit renderer / manifest sha pins.
.agents/skills/
.claude/skills/
platform/agents/
docs/private/
