apiVersion: v1
kind: Service
metadata:
  name: adguard
  namespace: utility-system
  labels:
    app.kubernetes.io/name: adguard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: adguard
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
