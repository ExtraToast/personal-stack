apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-ui
  namespace: auth-system
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll
    keel.sh/pollSchedule: '@every 2m'
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: auth-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: auth-ui
    spec:
      nodeSelector:
        personal-stack/site: frankfurt
      containers:
        - name: auth-ui
          image: ghcr.io/extratoast/personal-stack/auth-ui:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          livenessProbe:
            httpGet:
              path: /
              port: http
          resources:
            requests:
              cpu: 100m
              memory: 96Mi
            limits:
              cpu: 300m
              memory: 192Mi
---
apiVersion: v1
kind: Service
metadata:
  name: auth-ui
  namespace: auth-system
spec:
  selector:
    app.kubernetes.io/name: auth-ui
  ports:
    - name: http
      port: 80
      targetPort: http
