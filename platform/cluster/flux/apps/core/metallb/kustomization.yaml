apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - source.yaml
  - release.yaml
# IPAddressPool and L2Advertisement now live in apps/metallb-config so
# they apply only after the MetalLB chart has installed the CRDs.
