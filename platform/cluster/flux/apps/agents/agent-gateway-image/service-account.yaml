# SA used by every runner Pod (created at runtime by assistant-api).
# No special RBAC — the runner does not talk to the k8s API itself;
# all orchestration goes through assistant-api which has its own SA
# with Pod CRUD in this namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: agent-runner
  namespace: agents-system
