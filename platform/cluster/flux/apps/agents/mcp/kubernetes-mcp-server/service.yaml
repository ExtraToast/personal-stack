apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp-server
  namespace: agents-system
spec:
  selector:
    app.kubernetes.io/name: kubernetes-mcp-server
  ports:
    - name: mcp
      port: 8080
      targetPort: mcp
