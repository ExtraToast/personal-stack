apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - vault-secrets-operator-sa.yaml
  - credentials
  - mcp
  - auth-bootstrap
  - refresh-ping
  - kb-install
  - network-policy
  - agent-gateway-image # shared image pull only; runner Pods are created at runtime by agents-api
  - rbac
