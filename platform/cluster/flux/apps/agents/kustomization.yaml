apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - credentials
  - auth-bootstrap
  - refresh-ping
  - kb-install
  - network-policy
  - agent-gateway-image # shared image pull only; runner Pods are created at runtime by assistant-api
  - rbac
