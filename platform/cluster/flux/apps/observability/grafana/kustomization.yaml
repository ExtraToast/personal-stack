apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# HelmRepository `grafana` is declared once at observability/helm-repositories.yaml
# and referenced by name from release.yaml.
resources:
  - release.yaml
  - grafana-datasources.yaml
  - platform-overview-dashboard.yaml
  - dashboards
