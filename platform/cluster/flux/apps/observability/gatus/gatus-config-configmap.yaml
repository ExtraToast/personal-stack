apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-config
  namespace: observability
data:
  config.yaml: |
    storage:
      type: sqlite
      path: /data/data.db
    ui:
      title: personal-stack status
      description: Uptime for services declared in platform/inventory/fleet.yaml.
      header: personal-stack
