apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied by its own Flux Kustomization (apps-observability-rules) that
# dependsOn apps-observability, so kube-prometheus-stack has installed
# the monitoring.coreos.com CRDs by the time these rules are applied.
resources:
  - platform-alerts.yaml
