entryPoints:
  web:
    address: ":80"
  dashboard:
    address: ":8080"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    swarmMode: false
    exposedByDefault: false

api:
  dashboard: true
  insecure: true

log:
  level: DEBUG
